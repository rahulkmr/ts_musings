enum Direction {
    Up = 1,
    Down,
    Left,
    Right,
}


enum StrDirection {
    Up = 'UP',
    Down = 'DOWN',
    Left = 'LEFT',
    Right = 'RIGHT',
}


enum Hetrogenous {
    No = 0,
    Yes = 'YES',
}

enum Shapes {
    Circle,
    Square,
}